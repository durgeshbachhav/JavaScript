"use strict"; // Enable strict mode to enforce modern JavaScript standards and catch common errors.

// Using console.log to print the result of an arithmetic operation.
// console.log(3 + 3);

// Using alert to show a message (usually in a browser context).
// alert(3 + 3);

// Declaring a variable hey and initializing it with a string value.
let hey = "Durgesh";

// Declaring a variable age and initializing it with a numerical value.
let age = 22;

// Declaring a variable isLoggedIn and initializing it with a boolean value.
let isLoggedIn = true;

// Declaring a variable state without initializing it (it will be undefined).

// Data types in JavaScript:
// - Number: Represents numeric values (e.g., 2, 3.14).
// - BigInt: Represents large integer values.
// - String: Represents text enclosed in double or single quotes.
// - Boolean: Represents true or false values.
// - null: Represents a deliberate absence of a value.
// - undefined: Represents an uninitialized variable.
// - Symbol: Represents a unique and immutable value.

// Logging the data types of null and undefined.
console.log(typeof null); // This is a quirk in JavaScript, it incorrectly returns "object" for null.
console.log(typeof undefined); // Returns "undefined" as expected.
